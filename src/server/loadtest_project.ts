
import type { GlovServerStartupOptions } from './loadtest';
import type { DataObject } from 'glov/common/types';

export function loadTestInit(argv: DataObject, opts: GlovServerStartupOptions): void {
  // Project specific: fill in `opts.exchange` here
}
